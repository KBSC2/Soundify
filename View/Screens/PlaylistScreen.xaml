<ResourceDictionary 
					xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:local="clr-namespace:View.Screens" 
					xmlns:DataContexts="clr-namespace:View.DataContexts" xmlns:sys="clr-namespace:System;assembly=mscorlib"
    x:Class="View.Screens.PlaylistScreen">
    <DataTemplate x:Key="PlaylistScreen">
        <!-- Start playlist screen -->
        <Grid x:Name="MainGrid">
            <Grid.DataContext>
                <DataContexts:PlaylistDataContext/>
            </Grid.DataContext>
            <Grid Margin="0,0,0,0" Height="131" VerticalAlignment="Top">
                <Grid.Background>
                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                        <GradientStop Color="#FF6AF76A"/>
                        <GradientStop Color="#FFFF7070" Offset="1"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <Label Content="{Binding Playlist.Name}" HorizontalAlignment="Left" Margin="35,10,0,0" VerticalAlignment="Top" FontSize="22" Foreground="White" Width="181"/>
                <TextBlock HorizontalAlignment="Left" Margin="35,53,0,0" Text="{Binding Playlist.Description}" TextWrapping="Wrap" VerticalAlignment="Top" Height="67" Width="266" Background="#00000000" Foreground="White"/>
                <Button Content="Play" HorizontalAlignment="Center" VerticalAlignment="Top" Height="38" Width="166" Background="#FF303651" Foreground="White" BorderBrush="{x:Null}" FontSize="18" Margin="0,10,0,0" Click="Play_Playlist_Button_Click" />
            </Grid>
            <ListView x:Name="SongList" Margin="0,178,0,0" Background="#FF303651" BorderBrush="#FF303651" Foreground="White" ItemsSource="{Binding PlaylistItems}" FontSize="16">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Titel" Width="200" DisplayMemberBinding="{Binding Song.Name}"/>
                        <GridViewColumn Header="Artist" Width="200" DisplayMemberBinding="{Binding Song.Artist}"/>
                        <GridViewColumn Header="Duration" Width="75" DisplayMemberBinding="{Binding Duration}"/>
                        <GridViewColumn Header="Added" Width="150" DisplayMemberBinding="{Binding Added}"/>
                    </GridView>
                </ListView.View>
	            <ListView.Resources>
		            <ContextMenu x:Key="ItemContextMenu">
			            <MenuItem Header="Remove song" Click="ListViewItem_RightClick_DeleteSong" CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Parent}" />
		            </ContextMenu>
	            </ListView.Resources>
                <ListView.ItemContainerStyle>
		            <Style TargetType="{x:Type ListViewItem}"  >
                        <EventSetter Event="MouseDoubleClick" Handler="SongRow_Click"/>
			            <Setter Property="Background" Value="#FF445399" />
			            <Setter Property="Foreground" Value="White" />
                        <Setter Property="ContextMenu" Value="{StaticResource ItemContextMenu}" />
                        <Style.Triggers>
				            <Trigger Property="IsMouseOver" Value="True">
					            <Setter Property="Background" Value="White" />
					            <Setter Property="Foreground" Value="Black"/>
				            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FF8390E4" />
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
			            </Style.Triggers>
                    </Style>
	            </ListView.ItemContainerStyle>
            </ListView>
            <Grid Margin="0,131,0,0" Background="#FF303651" Height="47" VerticalAlignment="Top">
                <Button  Click="RemovePlaylistButton_Click" Tag="{Binding Playlist.ID}"  Content=" Remove playlist " HorizontalAlignment="Left" Margin="10,10,10,10" VerticalAlignment="Top" Background="{x:Null}" Foreground="White" BorderBrush="White" FontSize="16"/>
                <Button Content=" /\ " Tag="{Binding ElementName=MainGrid}"  HorizontalAlignment="Left" Margin="152,0,0,0" VerticalAlignment="Center" Background="#FF363F63" Foreground="White" Click="MoveUp_Click" Height="25" Width="25" BorderBrush="White"/>
	            <Button Content=" \/ " Tag="{Binding ElementName=MainGrid}" HorizontalAlignment="Left" Margin="185,0,0,0" VerticalAlignment="Center" Background="#FF363F63" Foreground="#FFF4F4F4" Click="MoveDown_Click" Height="25" Width="25" BorderBrush="White"/>
            </Grid>
        </Grid>
    </DataTemplate>
</ResourceDictionary>